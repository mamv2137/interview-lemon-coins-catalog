import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    alignItems: 'center',
    borderColor: 'red',
    borderWidth: 2,
    borderRadius: 8,
    padding: 12,
    gap: 8,
  },
  image: {
    height: 30,
    width: 30,
  },
  content: {
    width: '88%',
  },
});
