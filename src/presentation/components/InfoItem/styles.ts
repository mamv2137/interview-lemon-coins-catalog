import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    padding: 14,
    gap: 4,
  },
  title: {
    fontSize: 22,
    fontWeight: 500
  },
  content: {
    fontSize: 14,
  },
});

export default styles;
