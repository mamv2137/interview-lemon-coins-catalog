import React from 'react'
import { SafeAreaView, Text, View } from 'react-native'

const LoadingScreen = () => {
  return (
    <SafeAreaView>
      <Text>Loading</Text>
    </SafeAreaView>
  );
};

export default LoadingScreen;
